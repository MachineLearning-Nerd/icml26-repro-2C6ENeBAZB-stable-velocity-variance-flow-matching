# Claim 2 — ImageNet FID/IS

**Outcome: inconclusive (CPU-infeasible).** The pinned source reports the Table-1 80-epoch SiT-XL/2 ImageNet values, but literal reproduction needs ImageNet training and 50K-sample FID/IS evaluation. That is unavailable under the local CPU/local GTX 1050-only policy. No source audit or reduced calculation is presented as a benchmark outcome.
