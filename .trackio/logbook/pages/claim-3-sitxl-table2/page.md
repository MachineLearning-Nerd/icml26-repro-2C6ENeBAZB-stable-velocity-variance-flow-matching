# Claim 3 — SiT-XL/2 Table 2

**Outcome: inconclusive (CPU-infeasible literal benchmark).** The anchored claim requires 675M-parameter SiT-XL/2 training at 100k and 400k iterations plus FID evaluation. That is not feasible on the permitted local CPU/GTX 1050 hardware. No remote compute or surrogate was used; therefore this logbook does not verify or falsify Table 2.
